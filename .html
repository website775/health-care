<!DOCTYPE html>
<html lang="en">
<head>
	<meta charset="UTF-8">
	<meta name="viewport" content="width=device-width, initial-scale=1.0">
	<meta name="theme-color" content="#102a43">
	<title>Forge Lab | Technical Club, SCE Supaul</title>
	<link rel="preconnect" href="https://fonts.googleapis.com">
	<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
	<link href="https://fonts.googleapis.com/css2?family=DM+Mono:wght@400;500&family=Manrope:wght@400;500;600;700;800&display=swap" rel="stylesheet">
	<style>
		:root {
			--navy: #102a43;
			--ink: #183b56;
			--muted: #627d98;
			--paper: #f5f7f2;
			--line: #d9e2ec;
			--orange: #f05d23;
			--yellow: #f7b928;
			--white: #ffffff;
			--mono: "DM Mono", monospace;
			--sans: "Manrope", sans-serif;
		}

		* { box-sizing: border-box; }
		html { scroll-behavior: smooth; }
		body { margin: 0; color: var(--ink); background: var(--paper); font-family: var(--sans); }
		a { color: inherit; text-decoration: none; }
		button { font: inherit; }
		.shell { width: min(1180px, calc(100% - 72px)); margin: 0 auto; }
		.mono { font-family: var(--mono); text-transform: uppercase; letter-spacing: .09em; font-size: 11px; }

		header { position: absolute; z-index: 3; top: 0; left: 0; width: 100%; color: var(--white); }
		.nav { height: 86px; display: flex; align-items: center; justify-content: space-between; border-bottom: 1px solid rgba(255,255,255,.2); }
		.brand { display: flex; align-items: center; gap: 12px; font-weight: 800; letter-spacing: -.04em; font-size: 18px; }
		.brand-mark { width: 33px; height: 33px; display: grid; place-items: center; background: var(--orange); color: white; font-family: var(--mono); font-size: 12px; transform: rotate(-8deg); }
		.nav-links { display: flex; align-items: center; gap: 34px; font-size: 13px; font-weight: 600; }
		.nav-links a { opacity: .86; transition: opacity .2s; }
		.nav-links a:hover { opacity: 1; }
		.nav-cta, .hero-cta { background: var(--yellow); color: var(--navy); padding: 13px 18px; font-size: 12px; font-weight: 800; border: 0; cursor: pointer; }
		.menu { display: none; background: none; border: 0; color: white; font-size: 25px; cursor: pointer; }

		.hero { min-height: 680px; color: var(--white); background: linear-gradient(90deg, rgba(16,42,67,.96) 0%, rgba(16,42,67,.86) 43%, rgba(16,42,67,.36) 100%), url('https://images.unsplash.com/photo-1535378917042-10a22c95931a?auto=format&fit=crop&w=1800&q=85') center/cover; display: flex; align-items: center; }
		.hero-grid { display: grid; grid-template-columns: 1.03fr .97fr; gap: 70px; align-items: center; padding-top: 72px; }
		.eyebrow { color: var(--yellow); margin-bottom: 21px; }
		.hero h1 { max-width: 660px; margin: 0; font-size: clamp(46px, 6vw, 82px); line-height: .97; letter-spacing: -.075em; }
		.hero h1 em { color: var(--yellow); font-style: normal; }
		.hero-copy { max-width: 490px; color: #d9e2ec; margin: 27px 0 34px; line-height: 1.75; font-size: 16px; }
		.hero-actions { display: flex; gap: 24px; align-items: center; }
		.hero-cta { padding: 16px 22px; }
		.text-link { color: white; font-size: 13px; font-weight: 700; border-bottom: 1px solid var(--yellow); padding-bottom: 4px; }
		.hero-note { border-left: 1px solid var(--yellow); padding-left: 24px; margin-left: auto; max-width: 240px; font-size: 12px; line-height: 1.7; color: #d9e2ec; }
		.hero-note strong { display: block; color: white; font-size: 25px; letter-spacing: -.05em; margin-bottom: 8px; }

		.ticker { background: var(--orange); color: white; overflow: hidden; }
		.ticker-inner { height: 46px; display: flex; align-items: center; gap: 44px; white-space: nowrap; }
		.ticker span { font-family: var(--mono); font-size: 10px; letter-spacing: .14em; }
		.ticker i { width: 6px; height: 6px; background: var(--yellow); transform: rotate(45deg); display: inline-block; }

		section { padding: 104px 0; }
		.section-head { display: flex; justify-content: space-between; align-items: end; margin-bottom: 44px; }
		.section-label { color: var(--orange); margin: 0 0 13px; }
		h2 { margin: 0; font-size: clamp(32px, 4vw, 51px); line-height: 1.02; letter-spacing: -.07em; max-width: 550px; }
		.section-intro { color: var(--muted); max-width: 330px; line-height: 1.7; font-size: 14px; }

		.projects { background: var(--white); }
		.project-grid { display: grid; grid-template-columns: repeat(3, 1fr); gap: 18px; }
		.project { min-height: 375px; padding: 25px; display: flex; flex-direction: column; justify-content: space-between; background: var(--navy); color: white; position: relative; overflow: hidden; }
		.project:nth-child(2) { background: #dce8e2; color: var(--navy); }
		.project:nth-child(3) { background: var(--orange); }
		.project-number { font-family: var(--mono); font-size: 11px; opacity: .7; }
		.project-shape { position: absolute; width: 175px; height: 175px; border: 1px solid rgba(255,255,255,.35); border-radius: 50%; right: -42px; top: 72px; }
		.project-shape:after { content: ""; position: absolute; inset: 25px; border: 1px solid rgba(255,255,255,.35); border-radius: 50%; }
		.project h3 { font-size: 25px; letter-spacing: -.05em; max-width: 210px; margin: 0 0 12px; }
		.project p { font-size: 12px; line-height: 1.65; max-width: 250px; opacity: .78; }
		.project-footer { display: flex; justify-content: space-between; align-items: center; font-family: var(--mono); font-size: 10px; opacity: .75; }
		.arrow { font-family: var(--sans); font-size: 22px; }

		.events { background: #f5f7f2; }
		.event-list { border-top: 1px solid var(--line); }
		.event { display: grid; grid-template-columns: 130px 1fr 150px 30px; gap: 22px; align-items: center; padding: 24px 0; border-bottom: 1px solid var(--line); }
		.event-date { color: var(--orange); font-family: var(--mono); font-size: 12px; }
		.event h3 { margin: 0 0 5px; font-size: 18px; letter-spacing: -.04em; }
		.event p { margin: 0; color: var(--muted); font-size: 12px; }
		.event-type { font-family: var(--mono); color: var(--muted); font-size: 10px; }
		.event-arrow { color: var(--orange); font-size: 21px; }

		.join { color: white; background: var(--navy); }
		.join-grid { display: grid; grid-template-columns: 1fr 1fr; gap: 90px; align-items: center; }
		.join h2 { color: white; }
		.join p { color: #b8c7d6; line-height: 1.8; font-size: 14px; max-width: 440px; }
		.join-form { display: grid; grid-template-columns: 1fr 1fr; gap: 14px; }
		.join-form input, .join-form select { width: 100%; padding: 15px 0; border: 0; border-bottom: 1px solid #486581; color: white; background: transparent; outline: 0; font: 13px var(--sans); }
		.join-form input::placeholder { color: #829ab1; }
		.join-form select { color: #829ab1; }
		.join-form .full { grid-column: 1 / -1; }
		.submit { grid-column: 1 / -1; justify-self: start; margin-top: 17px; padding: 15px 22px; border: 0; background: var(--yellow); color: var(--navy); font-size: 12px; font-weight: 800; cursor: pointer; }
		.success { grid-column: 1 / -1; color: var(--yellow); font-family: var(--mono); font-size: 11px; display: none; }

		footer { background: #0b2236; color: #a9bac9; padding: 28px 0; font-size: 11px; }
		.footer-inner { display: flex; justify-content: space-between; align-items: center; }
		.footer-brand { color: white; font-weight: 800; }
		.socials { display: flex; gap: 20px; font-family: var(--mono); font-size: 10px; }

		@media (max-width: 760px) {
			.shell { width: min(100% - 40px, 560px); }
			.nav { height: 72px; }
			.nav-links { display: none; position: absolute; left: 20px; right: 20px; top: 80px; padding: 20px; background: var(--navy); flex-direction: column; align-items: flex-start; gap: 19px; }
			.nav-links.open { display: flex; }
			.nav-cta { display: none; }
			.menu { display: block; }
			.hero { min-height: 710px; }
			.hero-grid, .join-grid { grid-template-columns: 1fr; gap: 35px; }
			.hero h1 { font-size: clamp(45px, 14vw, 68px); }
			.hero-note { margin: 5px 0 0; }
			section { padding: 75px 0; }
			.section-head { display: block; }
			.section-intro { margin-top: 20px; }
			.project-grid { grid-template-columns: 1fr; }
			.project { min-height: 280px; }
			.event { grid-template-columns: 82px 1fr 20px; gap: 10px; }
			.event-type { display: none; }
			.join-grid { gap: 20px; }
			.join-form { grid-template-columns: 1fr; }
			.join-form .full, .submit, .success { grid-column: auto; }
			.footer-inner { display: block; line-height: 2; }
			.socials { margin-top: 10px; }
		}
	</style>
</head>
<body>
	<header>
		<nav class="nav shell">
			<a class="brand" href="#top"><span class="brand-mark">FL</span> FORGE LAB</a>
			<div class="nav-links" id="navLinks">
				<a href="#projects">Projects</a>
				<a href="#events">Events</a>
				<a href="#about">About us</a>
			</div>
			<a class="nav-cta" href="#join">Join the lab <span aria-hidden="true">↗</span></a>
			<button class="menu" id="menuButton" aria-label="Open menu" aria-expanded="false">☰</button>
		</nav>
	</header>

	<main id="top">
		<section class="hero">
			<div class="hero-grid shell">
				<div>
					<div class="eyebrow mono">SCE SUPAUL / EST. 2024</div>
					<h1>Build things that <em>matter.</em></h1>
					<p class="hero-copy">Forge Lab is the technical community at Supaul College of Engineering. A hands-on space for curious minds, bold ideas, and the next thing worth making.</p>
					<div class="hero-actions"><a class="hero-cta" href="#join">Become a member <span aria-hidden="true">↗</span></a><a class="text-link" href="#projects">See our work</a></div>
				</div>
				<div class="hero-note"><strong>01 — 04</strong>Four disciplines. One student-led community. Come with a question, leave with a working prototype.</div>
			</div>
		</section>

		<div class="ticker"><div class="ticker-inner shell"><span>Robotics</span><i></i><span>Web &amp; App</span><i></i><span>Design systems</span><i></i><span>Open source</span><i></i><span>Robotics</span></div></div>

		<section class="projects" id="projects">
			<div class="shell"><div class="section-head"><div><p class="section-label mono">Selected work / 2025</p><h2>Small teams.<br>Real outcomes.</h2></div><p class="section-intro">Our best learning happens when an idea leaves the notebook. Here are a few things we have put into the world.</p></div>
				<div class="project-grid">
					<article class="project"><span class="project-number">PROJECT 01</span><span class="project-shape"></span><div><h3>AgriSense</h3><p>A low-cost soil monitoring kit for the farms around Supaul, built with IoT and a lot of field testing.</p></div><div class="project-footer"><span>HARDWARE / IOT</span><span class="arrow">↗</span></div></article>
					<article class="project"><span class="project-number">PROJECT 02</span><span class="project-shape"></span><div><h3>Campus OS</h3><p>An open student portal that makes campus life a little easier to navigate, together.</p></div><div class="project-footer"><span>WEB / OPEN SOURCE</span><span class="arrow">↗</span></div></article>
					<article class="project"><span class="project-number">PROJECT 03</span><span class="project-shape"></span><div><h3>RoboRally</h3><p>Autonomous line followers, friendly rivalries, and the annual race that fills the lab with noise.</p></div><div class="project-footer"><span>ROBOTICS / EVENTS</span><span class="arrow">↗</span></div></article>
				</div>
			</div>
		</section>

		<section class="events" id="events"><div class="shell"><div class="section-head"><div><p class="section-label mono">Next up / Calendar</p><h2>There is always<br>something on.</h2></div><p class="section-intro">No gatekeeping, no prior experience needed. Just bring your curiosity.</p></div>
			<div class="event-list">
				<a class="event" href="#join"><span class="event-date">SEP 12<br>2025</span><div><h3>Build Night: Arduino 101</h3><p>Start here. Make your first sensor speak.</p></div><span class="event-type">WORKSHOP</span><span class="event-arrow">↗</span></a>
				<a class="event" href="#join"><span class="event-date">SEP 20<br>2025</span><div><h3>Designing for Bharat</h3><p>A product design sprint with local context.</p></div><span class="event-type">SPRINT</span><span class="event-arrow">↗</span></a>
				<a class="event" href="#join"><span class="event-date">OCT 04<br>2025</span><div><h3>RoboRally ’25</h3><p>Our annual build, break, and race day.</p></div><span class="event-type">COMPETITION</span><span class="event-arrow">↗</span></a>
			</div>
		</div></section>

		<section class="join" id="join"><div class="join-grid shell"><div id="about"><p class="section-label mono">Your turn / Join us</p><h2>Make your mark<br>at SCE.</h2><p>Whether you are here to code, design, organise, or simply figure out how things work, there is a place for you in Forge Lab.</p></div><form class="join-form" id="joinForm"><input aria-label="Your name" placeholder="Your name" required><input aria-label="Email address" type="email" placeholder="College email" required><select aria-label="Your interest" required><option value="" disabled selected>What interests you?</option><option>Robotics &amp; hardware</option><option>Web &amp; app development</option><option>Design &amp; content</option><option>Events &amp; community</option></select><input aria-label="Year of study" placeholder="Year of study" required><button class="submit" type="submit">Send application <span aria-hidden="true">↗</span></button><div class="success" id="success">APPLICATION RECEIVED — WE WILL BE IN TOUCH.</div></form></div></section>
	</main>
	<footer><div class="footer-inner shell"><div><span class="footer-brand">FORGE LAB</span> / Technical Club, SCE Supaul</div><div class="socials"><a href="#top">INSTAGRAM</a><a href="#top">GITHUB</a><a href="#top">LINKEDIN</a></div></div></footer>
	<script>
		const menuButton = document.getElementById('menuButton');
		const navLinks = document.getElementById('navLinks');
		menuButton.addEventListener('click', () => {
			const open = navLinks.classList.toggle('open');
			menuButton.setAttribute('aria-expanded', open);
		});
		navLinks.querySelectorAll('a').forEach((link) => link.addEventListener('click', () => navLinks.classList.remove('open')));
		document.getElementById('joinForm').addEventListener('submit', (event) => {
			event.preventDefault();
			document.getElementById('success').style.display = 'block';
			event.target.reset();
		});
	</script>
</body>
</html>
